WE BOARD không chạy được các truy vấn sau:

{% for failure in failures %}

{{failure.name}} ({{base_url}}/queries/{{failure.id}})
Lần lỗi gần nhất: {{failure.failed_at}}{% if failure.failure_count > 1 %} + {{failure.failure_count - 1}} lần lỗi nữa kể từ báo cáo trước{% endif %}
Lỗi:

{{failure.failure_reason}}

{% if failure.comment %}
{{failure.comment}}
{% endif %}

{% endfor %}